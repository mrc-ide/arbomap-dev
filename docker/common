#!/usr/bin/env bash
REGISTRY=ghcr.io
PACKAGE_ORG=mrc-ide
PACKAGE_NAME=grout
PACKAGE_VERSION=poc
TAG="${REGISTRY}/${PACKAGE_ORG}/${PACKAGE_NAME}:${PACKAGE_VERSION}"

NAME="${PACKAGE_NAME}-${PACKAGE_VERSION}"